-- Configuration
function love.conf(w)
	w.title = "Tetris in Love" -- The title of the window the game is in (string)
	w.version = "0.9.2"         -- The LÖVE version this game was made for (string)
	w.window.width = 480        -- we want our game to be long and thin.
	w.window.height = 640
end
